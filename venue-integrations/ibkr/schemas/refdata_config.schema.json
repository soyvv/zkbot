{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "IBKR Refdata Config",
  "description": "Venue-specific configuration for the IBKR refdata loader.",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "enum": ["paper", "live"],
      "default": "paper",
      "description": "IBKR connection mode. This is metadata for operator intent; host and port still determine the actual TWS or IB Gateway endpoint."
    },
    "host": {
      "type": "string",
      "default": "127.0.0.1",
      "description": "Hostname or IP address of the target TWS or IB Gateway session."
    },
    "port": {
      "type": "integer",
      "minimum": 1,
      "default": 7497,
      "description": "TCP port of the target TWS or IB Gateway session."
    },
    "client_id": {
      "type": "integer",
      "minimum": 0,
      "default": 10,
      "description": "IBKR API client ID used for the refdata session."
    },
    "account_code": {
      "type": "string",
      "default": "",
      "description": "Optional IBKR account code for account-scoped lookups."
    },
    "universe": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "List of IB contract specification strings to qualify, for example AAPL-USD-STK-SMART."
    },
    "read_only": {
      "type": "boolean",
      "default": true,
      "description": "Refdata adaptor safety default. Present for parity with the shared IBKR config model."
    },
    "reconnect_delay_s": {
      "type": "number",
      "minimum": 0.1,
      "default": 2,
      "description": "Initial reconnect backoff delay in seconds."
    },
    "reconnect_max_delay_s": {
      "type": "number",
      "minimum": 1,
      "default": 60,
      "description": "Maximum reconnect backoff delay in seconds."
    },
    "next_valid_id_timeout_s": {
      "type": "number",
      "minimum": 1,
      "default": 10,
      "description": "Timeout in seconds waiting for the nextValidId handshake after connect."
    }
  },
  "required": ["mode", "host", "port", "client_id"],
  "additionalProperties": true
}
