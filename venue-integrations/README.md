Venue integration module placeholders described in `docs/system-arch/venue_integration.md`.

Each venue module owns:
- `manifest.yaml`
- `python/` entrypoints for venue-specific adaptors
- `schemas/` config validation stubs

These are placeholders only. The generic service hosts remain in Rust.
