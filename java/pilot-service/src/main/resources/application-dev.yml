spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/zkbot
    username: zk
    password: zk
  data:
    redis:
      url: redis://localhost:6379

pilot:
  nats-url: nats://localhost:4222
  env: dev
  pilot-id: pilot_dev_1
  venue-integrations-root: ../venue-integrations
  service-manifests-root: ../service-manifests
  # Must be an absolute, executable path. Makefile-driven dev sets
  # PILOT_ENGINE_BINARY_PATH to $(CURDIR)/rust/target/debug/zk-engine-svc.
  engine-binary-path: ${PILOT_ENGINE_BINARY_PATH}
  engine-log-dir: ../devops/logs
